---
project: engineer-agents
ticket: 2
title: Add Engineer entry to sub-agent invocation
phase: 6
type: ticket
status: complete
created: 2026-04-15T23:50:00-03:00
estimated_minutes: 15
depends_on: []
---

**Wave:** Wave 1 — Foundation

**What:** Add the Engineer sub-agent invocation entry to `instructions/sub-agent-invocation.md`, following the existing numbered pattern (currently 1-7, so the Engineer becomes entry 8).

**Why:** The sub-agent invocation file is the canonical registry of how the Pal calls each agent. Without this entry, the Pal has no contract for invoking the Engineer.

**Owner:** builder

**Allowed writes:**
- `instructions/sub-agent-invocation.md`

**Verification:**
- `instructions/sub-agent-invocation.md` contains a new `### 8. Engineer` section
- The section follows the existing pattern: description paragraph, code block with `Agent(Engineer):` showing input/output
- Input specifies: `prompts/engineer-agent.md` + ticket content (inline) + project context
- Output specifies: completed ticket work + status update + blocker report (if any)
- The entry states the Engineer is invoked at Phase 7

**Done when:** A `### 8. Engineer` section exists in `instructions/sub-agent-invocation.md` with the invocation contract matching the pattern of entries 1-7.

**Notes:**
- Source: Brief scope item "Execution contract: how the Engineer consumes tickets, signals completion, and reports blockers"
- The existing entries follow a consistent pattern: number, name, "Invoked at Phase N", then a fenced code block. Match that exactly.
- The Brief specifies batch completion: Engineer hands back to Pal after completing all wave tickets.
- Do NOT add parallel execution details here — that is ticket 004.
