---
project: engineer-agents
ticket: 3
title: Add blocker handling protocol to Engineer prompt
phase: 6
type: ticket
status: complete
created: 2026-04-15T23:50:00-03:00
estimated_minutes: 15
depends_on: [1]
---

**Wave:** Wave 1 — Foundation

**What:** Add a `## Blocker Handling` section to `prompts/engineer-agent.md` that defines the structured blocker protocol: try up to 3 approaches, then stop with a structured report.

**Why:** The Engineer must handle blockers predictably so the user gets a clear report instead of a vague pause. This is a core success criterion from the Brief.

**Owner:** builder

**Allowed writes:**
- `prompts/engineer-agent.md`

**Verification:**
- `prompts/engineer-agent.md` contains a `## Blocker Handling` section
- Section defines the 3-attempt threshold (the Brief's suggested default)
- Section specifies the structured blocker report format with at minimum: blocker description, what was tried (up to 3 approaches), what decision is needed from the user
- The Workflow section references the blocker protocol (e.g., "If blocked, follow the Blocker Handling protocol")
- Anti-patterns section includes: never silently skip a blocked task, never exceed the attempt threshold without stopping

**Done when:** The Engineer prompt at `prompts/engineer-agent.md` contains the Blocker Handling section with the 3-attempt threshold and structured report format, and the Workflow and Anti-patterns sections reference it.

**Notes:**
- Source: Brief scope item "Blocker handling protocol (try N approaches, then stop with structured report)"
- The Brief's open question resolved: default threshold is 3 attempts.
- The blocker report should be structured enough that the Pal can surface it to the user cleanly. Suggest a fenced template block in the prompt.
- Keep the section concise — this is a ~15-min persona prompt addition, not an error-handling framework.
