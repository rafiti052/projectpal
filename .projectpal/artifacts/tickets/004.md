---
project: engineer-agents
ticket: 4
title: Add parallel execution guidance to sub-agent invocation
phase: 6
type: ticket
status: complete
created: 2026-04-15T23:50:00-03:00
estimated_minutes: 15
depends_on: [2]
---

**Wave:** Wave 2 — Pipeline Integration

**What:** Extend the Engineer entry in `instructions/sub-agent-invocation.md` with parallel execution guidance: when to spawn multiple Engineer agents within a wave, how `allowed_writes` non-overlap is the gating condition, and how the Pal orchestrates wave-level parallelism.

**Why:** The Brief requires parallel execution when tickets have no dependency conflicts and non-overlapping `allowed_writes`. The sub-agent invocation file needs to document this so the Pal knows how to spawn and manage concurrent Engineers.

**Owner:** builder

**Allowed writes:**
- `instructions/sub-agent-invocation.md`

**Verification:**
- The `### 8. Engineer` section in `instructions/sub-agent-invocation.md` now includes parallel execution rules
- Rules specify: spawn one Engineer per ticket within a wave when `depends_on` chains are satisfied and `allowed_writes` do not overlap
- Rules specify: fall back to sequential execution when dependencies or write surfaces overlap
- Rules specify: each Engineer instance only writes within its ticket's `allowed_writes` scope
- Rules specify: the Pal orchestrates wave lifecycle — Engineer hands back after completing its ticket
- The guidance is consistent with the existing Phase 7 protocol language about parallelism

**Done when:** The Engineer entry in `instructions/sub-agent-invocation.md` contains parallel execution rules that define when parallel spawning is safe, when sequential fallback applies, and how the Pal manages the wave lifecycle.

**Notes:**
- Source: Brief scope item "Parallel execution: spawn one Engineer agent per ticket within a wave"
- Reference the existing Phase 7 protocol language: "Within a wave, parallelize only the tickets whose `depends_on` chain is satisfied and whose `allowed_writes` do not overlap on an exclusive write surface."
- The Brief's open question on batch boundaries resolved: Engineer hands back to Pal after completing all wave tickets.
- Be mindful of the lean v1 parallel delegation guard already in `sub-agent-invocation.md` — the Engineer parallel guidance should note that this is a Phase 7 orchestration pattern, not a general parallel delegation override.
