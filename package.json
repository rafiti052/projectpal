{
  "name": "projectpal",
  "version": "0.4.0",
  "description": "ProjectPal runtime — connector orchestration layer",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check:install": "tsx scripts/check-install.ts --fixture",
    "test:integration": "sh scripts/test-integration.sh"
  },
  "dependencies": {
    "yaml": "^2.8.3"
  },
  "devDependencies": {
    "@types/node": "^22.19.17",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
