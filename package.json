{
  "name": "projectpal",
  "version": "0.4.1",
  "description": "ProjectPal runtime — workflow continuity, install checks, and optional delegated-execution helpers",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "check:install": "tsx scripts/check-install.ts",
    "test:integration": "sh scripts/test-integration.sh"
  },
  "dependencies": {
    "yaml": "^2.8.3"
  },
  "devDependencies": {
    "@types/node": "^22.19.17",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
